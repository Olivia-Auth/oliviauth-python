# OliviaAuth Python SDK - Required Dependencies

# HTTP client
requests>=2.28.0

# Cryptography (RSA, AES-GCM)
cryptography>=40.0.0

# WebSocket support (required for socket mode and remote commands)
python-socketio[client]>=5.8.0
websocket-client>=1.5.0
